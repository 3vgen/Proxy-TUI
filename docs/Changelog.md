# Хронология работ (2026-09-07)

Все работы выполнялись удалённо с Mac через `ssh homeserver`.

1. **Папка ~/proxy** — создана на сервере под задачу «прокси/VPN».
2. **Подписка получена и разобрана**: base64 → 14 строк `vless://` Reality (позже состав менялся до 15).
3. **Тест узлов (SOCKS-метод)**: временный sing-box с socks-inbound проверял реальный egress каждого узла (ipinfo). Выяснилось: часть узлов падает `reality verification failed`, у РФ-узлов egress RU.
4. **Выбран рабочий зарубежный узел** для старта: `#14 BE 113.30.152.105` (позже стал `#13`). Через него открылись api.anthropic.com / api.openai.com / docker.io.
5. **Установка sing-box 1.14.0** → `/usr/local/bin`, конфиг в `/etc/sing-box/config.json`, **systemd-сервис `sing-box`** (enabled).
6. **TUN-конфиг** (auto_route, strict_route) запущен, но интернет через тун молчал.
   - Отладка по шагам: ip rules → таблица 2022 → route_exclude узлов (self-loop) → **DNS-формат 1.14** → **UFW allow in on tun0** (главная причина).
7. **Проверка**: egress BE, Anthropic 404 / OpenAI 421 / docker 401, `docker pull hello-world` — OK.
8. **Безопасный toggle**: `systemctl stop` → прямой интернет (публичный IP роутера), `start` → VPN. Проверено.
9. **Скрипты** `vpn-on.sh` / `vpn-off.sh` (пароль читается из `~/.sudo_password`).
10. **CLI `vpn`** в папке проекта: list/ping/test/speed/use/status/refresh + интерактив. urltest-`auto` фильтрует только не-RU рабочие узлы.
11. **Миграция папки**: `~/proxy` → `~/vpn-tool` (папка проекта). Пароль sudo и сервис остались системными.
12. **Итоговое состояние**: `use auto` → egress NL Amsterdam (144.31.24.107); рабочие узлы: #2 NL, #13 BE; скорость #13 ≈ 24.7 Mbps.
13. **База знаний**: эта папка `docs/`.

## 2026-09-07 (вечер, доработка агентом)

1. **Параллельное тестирование узлов** в CLI `vpn`: `test all` теперь ~10 с
   вместо ~3.5 мин (раньше 15 узлов × ~14 с последовательно). Каждому узлу —
   свой socks-порт `10800+i` и свой временный конфиг (авто-чистка).
2. **Новые команды**:
   - `vpn use best` — выбрать лучший рабочий зарубежный узел (скорость →
     задержка) и применить статично.
   - `vpn history` — история тестов (`cache/history.jsonl`: страна/IP/мс/скорость
     по времени).
   - `vpn autocheck` — тест всех + восстановление (`use auto`→`use best`) при
     деградации egress; exit-code для cron.
   - `vpn cron install|remove` — ежечасная авто-проверка в crontab
     (лог `cache/autocheck.log`).
3. **`vpn status` расширен**: показывает target (auto/#N) + egress
   (IP/country/city/org), а не только IP.
4. **Авто-ротация по крону установлена** (`0 * * * *`, hourly autocheck).
5. **Интерактивный TUI переписан на Textual** (заменил curses-версию, которая
   глючила): полноэкранный список узлов, навигация стрелками, `Enter` —
   подключение, `p`/`P` — ping с анимацией (одиночный/все), `t`/`s` — тест/
   скорость, `a`/`b` — auto/best, `o` — **вкл/выкл VPN**, `r` — обновление,
   `q` — выход. Статусная строка показывает, к какому узлу подключен и egress.
   Textual установлен в venv `.venv/`; код разделён на `vpntool.py` (логика) +
   `vpn` (CLI) + `vpntui.py` (TUI).
6. **Диагностика «мёртвых» узлов уточнена** (раньше просто «reality-ошибка»):
   - `#3/9/11` (`201.34.132.43`, sni `ozone.ru`): `x509 ... not ozone.ru` —
     SNI не совпадает с сертификатом; перебор альтернативных SNI не помог →
     дальше `reality verification failed` (ключи на стороне провайдера, чинить
     нельзя клиентски).
   - `#4/6/7/10/12`: `reality verification failed` — pbk/sid не совпадают с
     сервером (stale/другая подписка).
   - `#14` (`81.17.159.174`): `connection refused` — сервер реально лежит.
   Итог: по-прежнему рабочие только `#2 NL` и `#13 BE` (зарубежные) + `#0/#1`
   (RU). Новых узлов клиентскими правками не разблокировать.

Известные ограничения на текущий момент:
- Только 2 из 15 узлов реально туннелируют зарубежный трафик (остальные — reality-ошибки/сервер лежит).
- Нет IPv6-покрытия в tun (только IPv4).
- `cache/history.jsonl` растёт без ротации (см. [[Roadmap]]).

## 2026-09-07 (поздний вечер) — фикс входящего SSH при включённом VPN

**Проблема (от пользователя):** «если на моём компьютере выключен VPN, а на сервере
включён — не могу зайти по SSH».

**Диагноз:** при `auto_route` весь не-private трафик уходит в tun0. Ответы на
входящие SSH-соединения уходят в тун с IP VPN-узла (асимметричный маршрут) →
рукопожатие не завершается. Раньше «работало» случайно: exit-IP VPN на Mac совпадал
с узлом подписки (`5.129.218.70` = узел #4), а IP узлов исключены из туна.

**Решение:**
1. Скрипт `~/vpn-tool/ssh-bypass.sh` (`install|uninstall|status`) — nftables-маркировка
   входящего SSH (`tcp dport 22 → ct mark 0x1`) + копирование в packet mark, и
   ip rule `8500: fwmark 0x1 lookup main` (pref < 9000, выигрывает у правил sing-box).
2. systemd-юнит `vpn-ssh-bypass.service` (oneshot, enabled) — ставит правило при
   загрузке; независим от sing-box, переживает `vpn use`/restart.
3. Проверено: `ip route get 8.8.8.8 mark 0x1` → `dev wlp1s0` (прямо), без mark →
   `dev tun0`; счётчики nft растут; SSH живёт при `systemctl restart sing-box`;
   egress остаётся VPN (113.30.152.105).

Обновлено: [[Infrastructure]], [[Networking-Troubleshooting]] (п.8), [[Home]],
[[Onboarding-Agent]].

## 2026-09-07 (ночь) — минималистичный TUI с кнопками

1. **Кнопки в TUI** (`vpntui.py`, Textual): убраны Header/Footer, добавлены
   кликабельные кнопки — сверху **«Выключить/Включить VPN»** (подпись и цвет
   меняются по состоянию: красный когда VPN включён, зелёный когда выключен),
   внизу ряд `Auto` / `Best` / `Обновить` / `Выход`. Кнопки мышиные
   (`can_focus=False`, фокус остаётся на таблице); клавиши (`o`, `a`, `b`, `r`, `q`)
   по-прежнему работают.
2. **Минимализм**: компактная статусная строка (`VPN ВКЛ/ВЫКЛ · #N · egress`),
   плоская таблица без рамки, одна строка-подсказка снизу.
3. Обновлено: [[CLI-Usage]].
